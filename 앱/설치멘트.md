# 메킷 키트 앱 판 — 수강생 설치 멘트 (승인글)

수강생 컴퓨터에 설치하는 건 없습니다. ChatGPT 앱(Plus 이상)만 있으면 됩니다.

## 준비 (처음 한 번)

1. ChatGPT 앱 설치 → Plus 계정 로그인 → 왼쪽 **Codex** 클릭
2. 바탕화면에 빈 폴더 하나 만들기 (이름 예: `메킷키트`)
3. 코덱스에서 **새 채팅** → 작업 폴더로 방금 만든 폴더 선택
4. 아래 한 문장을 채팅창에 치고 엔터

## 설치 — 채팅창에 이 한 문장만 (실측 2026-09-20 통과)

```
깃허브 makeit-edu/makeit-middle-kit 저장소의 README 를 읽고 거기 적힌 대로 설치해
```

코덱스가 저장소 첫 화면을 읽고 알아서 설치합니다. 30초쯤 걸립니다.

> "README 를 읽고" 가 꼭 들어가야 합니다. 주소만 주고 "설치해" 라고 하면 코덱스가 저장소를 통째로 내려받으려다 실패합니다 (실측).

### 위 문장이 안 될 때만 — 예비 멘트 (통째로 붙여넣기)

```
MCP 서버 node_repl 의 js 도구에서 아래 코드를 한 글자도 바꾸지 말고 실행하고 출력을 그대로 보여줘라. 터미널 명령은 쓰지 마라.

let fs = await import("node:fs/promises"); let os = await import("node:os"); let path = await import("node:path");
let 작업폴더 = path.resolve(".");
let 임시 = await fs.mkdtemp(path.join(os.tmpdir(), "makeit-setup-"));
let r = await fetch(atob("aHR0cHM6Ly9yYXcuZ2l0aHVidXNlcmNvbnRlbnQuY29tL21ha2VpdC1lZHUvbWFrZWl0LW1pZGRsZS1raXQvbWFpbi8lRUMlOTUlQjEvJUVDJTg0JUE0JUVDJUI5JTk4Lm1qcw==") + "?t=" + Date.now());
await fs.writeFile(path.join(임시, "설치.mjs"), await r.text());
let 결과 = await (await import("file://" + path.join(임시, "설치.mjs") + "?t=" + Date.now())).설치({ 작업폴더 });
await fs.rm(임시, { recursive: true, force: true });
nodeRepl.write(JSON.stringify(결과, null, 1));
```

`결과: "설치 끝"` 이 나오면 끝입니다. 이 채팅을 닫고 **같은 폴더로 새 채팅**을 연 다음 이렇게 말하세요.

```
승인글 자동화 시작해
```

## 그다음은 코덱스가 묻는 대로 답만 하면 됩니다

| 코덱스가 묻는 것 | 언제 | 답 |
|---|---|---|
| 수강 코드 | 처음 한 번 | 자료실 공지의 코드 |
| OpenAI API 키 | 처음 한 번 | sk- 로 시작하는 키 (받는 법: makeit-edu.github.io/makeit-middle-kit/key.html) |
| 충전한 금액(달러) | 처음 한 번 | 예: 10 (모르면 "몰라요") |
| 워드프레스 정보 3가지 | 사이트마다 한 번 | 주소 · 관리자 아이디 · 애플리케이션 비밀번호 |
| 제목 | 제목이 떨어졌을 때 | 1개든 100개든 한 줄에 하나씩 |
| 몇 개 만들지 | 매번 | "1개" → 확인 후 "10개" |

글 하나가 끝날 때마다 **게이지**와 **💰 이 글에 쓴 토큰·돈 · 지금까지 · 남은 돈**이 나옵니다.
글은 워드프레스 **임시글**로만 저장됩니다. 확인한 뒤 발행 버튼은 직접 누르세요.

## 폴더에 남는 것

- `AGENTS.md` — 코덱스 대본 (건드리지 마세요)
- `애드센스 승인글/00_설정/설정.json` — 내 키·사이트 정보 (남에게 보내지 마세요)
- `애드센스 승인글/01_제목넣는곳/` — 제목 파일
- `애드센스 승인글/02_생성결과_확인용/` — 만든 글, 사용량 기록

프로그램 파일은 폴더에 남지 않습니다. 실행할 때마다 GitHub 에서 최신판을 읽어 옵니다.

## 막히면

코덱스에게 "진단해줘" 라고 하세요. 설정 상태, 사이트 연결, 지금까지 만든 글과 쓴 돈이 나옵니다. 그 화면을 글자로 복사해 문의 채널에 올리면 됩니다 (키는 가려져 나옵니다).
