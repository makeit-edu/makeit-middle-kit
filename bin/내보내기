#!/usr/bin/env bash
# 내보내기 — 완성영상 폴더의 mp4를 zip 하나로 묶어 한 번에 내려받게 합니다. (2주차 7단계)
set -u

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PROGRAM_DIR="$ROOT/99_절대_건들지마세요_프로그램파일"

echo "[내보내기] 완성영상을 zip 하나로 묶습니다..."
exec npm --prefix "$PROGRAM_DIR" run video:export -- "$@"
