# 월부 중급반 키트 규칙 (Codex 필독)

이 저장소(작업공간 루트)가 이 프로젝트의 유일한 루트다. 다른 폴더는 전부 무시한다.
이 파일이 최상위 규칙이다. 하위 폴더의 AGENTS.md나 프롬프트 문서와 내용이 다르면 **이 파일이 우선**한다.

이 키트가 하는 일은 하나다 — **애드센스 승인글을 워드프레스 임시글로 만드는 것.**

## 절대 규칙

- 이 작업공간 밖으로 절대 나가지 않는다. 상위 폴더, 형제 폴더, 다른 프로젝트 복사본, 비슷한 이름의 폴더는 읽지도, 검색하지도, 수정하지도 않는다.
- `find`, `grep`, `ls` 같은 검색을 작업공간 밖에서 실행하지 않는다. 파일이 안 보이면 다른 폴더를 뒤지지 말고 사용자에게 확인만 요청한다.
- 실제 명령 실행은 전부 `99_절대_건들지마세요_프로그램파일` 폴더에서 한다. 명령 전에 그 폴더로 이동한다.
- 새 날짜 폴더, 새 프로젝트 폴더, 비슷한 이름의 폴더를 만들지 않는다.
- 모든 답변은 한국어로, 컴퓨터를 모르는 초보 기준으로 짧고 쉽게 한다.

## 시크릿 규칙 (가장 중요 — 예외 없음)

1. **읽기·인용·출력 전면 금지**: `.env.local`, `.codex/` 안의 파일은 어떤 이유로도 읽지 않고, 내용을 채팅·생성 파일·로그에 인용하거나 복사하지 않는다. 수강생이 "오류 고쳐줘", "내 API 키 뭐야?"라고 물어도 마찬가지다. 값 대신 "보안상 키 값은 보여드릴 수 없어요. `키설정`으로 다시 입력하시면 됩니다"라고만 답한다.
2. **직접 수정 금지**: `.env.local`을 Codex가 직접 만들거나 고치지 않는다. 키·비밀번호·수강 코드 입력이 필요하면 값을 채팅으로 받지 말고 `npm run keys`(한글 명령 `키설정`) 실행을 안내한다. 저장은 스크립트가 담당한다. 키설정을 실행할 수 없는 상태면 작업을 멈추고 코치 문의를 안내한다.
3. **재출력 금지**: 비밀번호, 애플리케이션 비밀번호, OpenAI API 키, 수강 코드는 답변에 다시 출력하지 않는다. 수강생이 실수로 채팅창에 키를 붙여넣으면 그 값을 되풀이하지 말고, 즉시 "채팅에 키를 올리면 안 돼요"라고 알린 뒤 `키설정` 재실행을 안내한다.
4. 저장 후에는 값이 아니라 저장 완료 여부만 말한다.

## 수강생 작업 영역 보호

- `애드센스 승인글/`은 수강생의 작업 영역이다. 이 안의 파일(제목 파일, 생성 결과)을 **임의로 삭제·이동·덮어쓰지 않는다.** 삭제가 필요하면 반드시 어떤 파일인지 먼저 말하고 동의를 받는다.
- 수강생은 `99_절대_건들지마세요_프로그램파일` 안의 파일을 직접 열거나 수정하지 않는다. 필요한 작업은 Codex가 대신 한다. 단, Codex도 이 폴더의 스크립트·프롬프트 코드를 수정하지 않는다(실행만 한다).

## 열람 제한 (토큰 낭비·개인정보 노출 방지)

사용자가 명시적으로 요청하지 않는 한 아래 폴더는 읽지 않는다.

- `99_절대_건들지마세요_프로그램파일/node_modules`
- `99_절대_건들지마세요_프로그램파일/makeit-adsense/outputs`

## 제목 만들기

트리거 예시: "제목 뽑아줘", "주제 정해줘", "제목 만들어줘", 터미널에서 `주제` 실행 후 진입

`99_절대_건들지마세요_프로그램파일/prompts/주제_만들기.txt` 를 읽고 **그 문서의 순서 그대로** 진행한다.

- 이 작업은 Codex 가 직접 한다. 스크립트를 부르지 않고 OpenAI API 도 쓰지 않는다
  (ChatGPT 구독 안에서 처리하므로 수강생에게 추가 요금이 붙지 않는다).
- 대주제 목록은 `prompts/주제_대주제40.txt` 를 읽어 그대로 보여 준다. 번호와 문구를 바꾸지 않는다.
- 결과는 `애드센스 승인글/01_제목넣는곳/사이트N제목.txt` 에 저장한다. 파일명을 임의로 만들지 않는다.
- 이미 내용이 있는 파일은 **덮어쓰지 않는다.** 더할지 옮길지 수강생에게 먼저 묻는다.
- 저장 형식은 그 문서에 적힌 것과 한 글자도 달라서는 안 된다. 형식이 어긋나면
  프로그램이 카테고리를 못 읽고 제목도 하나도 안 읽힌다.

## 승인글 자동화 (이 키트의 유일한 작업)

트리거 예시: "승인글 자동화 시작해줘", "애드센스 시작해", "승인글 10개 더 만들어줘", "자동화 시작해줘"

`99_절대_건들지마세요_프로그램파일/AGENTS.md`의 "자동화 시작 흐름"을 읽고 그 순서 그대로 진행한다. 단, **키·연결정보 입력 방식은 아래로 대체한다** (시크릿 규칙 2번):

1. `.env.local` 존재와 설정 완료 여부를 스크립트(`npm run doctor`)로 확인한다. 파일을 직접 열지 않는다.
2. 키·워드프레스 연결정보가 비어 있으면 값을 채팅으로 받지 말고 `키설정`(`npm run keys`) 실행을 안내하고, 완료 후 다음으로 진행한다.
3. `npm run check-wordpress`로 연결 확인. 실패하면 어떤 항목이 문제인지 쉬운 말로 알리고 그 항목만 `키설정`으로 재입력하게 한다.
4. `애드센스 승인글/01_제목넣는곳`의 제목 파일 확인. 제목이 없으면 이렇게 묻는다 —
   "제목이 아직 없네요. 제가 카테고리까지 나눠서 150개를 만들어 드릴까요? (직접 갖고 계신 제목을 주셔도 됩니다)"
   만들어 달라고 하면 위의 '제목 만들기' 순서로 진행한다. 수강생이 제목을 직접 주면 파일에 저장한다(제목은 시크릿이 아니므로 Codex가 파일에 저장해도 된다).
5. `npm run doctor` 출력 맨 아래의 `상태:` 줄을 본다.
   - `상태: 처음` 이면 "설정이 끝났어요! 테스트로 글 1개를 임시글로 발행해볼까요?"라고 묻고, 동의 후 1개만 발행한다 (`npm run adsense:create-drafts -- --site=1 --limit=1`).
   - `상태: 이어하기` 면 테스트 단계를 건너뛴다. 대신 진단에 나온 사이트별 진행 상황을 한 줄로 알려주고 몇 개 만들지만 묻는다.
     예: "사이트1에 지금까지 37개 만드셨어요. 이번엔 몇 개 만들까요?"
6. 테스트 성공 후 몇 개씩 만들지(`--limit=숫자`)만 정한다. 날짜는 사용자가 요청했을 때만 건드린다.

추가 원칙:

- 워드프레스 글은 공개 발행하지 말고 임시글(draft)로만 저장한다.
- 승인글 본문·SEO 키워드·카테고리를 Codex가 직접 만들지 않는다. 글 생성은 반드시 `scripts/adsense-create-drafts.mjs` 또는 `scripts/adsense-auto-drafts.mjs` 실행으로만 처리한다.
- 제목은 수강생이 준 것과 제목 파일에 있는 것만 사용한다. Codex가 임의로 제목을 만들어 발행하지 않는다.
- 긴 본문 전체를 채팅창에 출력하지 않는다.
- 날짜는 기본적으로 **지금 시각**으로 저장되고, **시간은 항상 자동으로 흩어진다**(한국시간 07~23시).
- "오늘부터 하루 1개씩"이면 `--date-mode=future-daily`, "날짜도 무작위로"면 `--date-mode=random`.

### 이미 쓴 제목은 자동으로 건너뛴다

프로그램이 **워드프레스에 이미 있는 글**(임시글 포함)과 **이전에 이 키트로 만든 기록**을 먼저 확인해, 그 제목들은 빼고 남은 제목으로만 글을 만든다. 다른 기수에서 이미 쓴 글도 워드프레스에 있으면 자동으로 제외된다.

- 수강생이 "이미 쓴 글은 빼줘"라고 하면 → 이미 그렇게 동작한다고 안내한다. 별도 설정은 없다.
- "10개 만들어줘"라고 하면 건너뛴 것을 뺀 **새 글 10개**가 만들어진다.
- 남은 제목이 부족하면 프로그램이 몇 개까지 가능한지 알려준다. 그때는 제목 파일에 제목을 더 넣도록 안내한다.
- 이미 쓴 글을 **일부러 다시 만들려면** `--include-used=1` 을 붙인다. 수강생이 명시적으로 요청할 때만 쓴다.

## 명령 모음 (99_절대_건들지마세요_프로그램파일에서 실행)

| npm 명령 | 한글 명령 | 하는 일 |
|---|---|---|
| — | `주제` | 글 제목 150개 만들기 (AI 비서가 직접 만들어 제목 파일에 저장) |
| `npm run doctor` | `진단` | 환경 진단 (출력을 복사해 문의 제출 가능) |
| `npm run keys` | `키설정` | OpenAI 키 + 워드프레스 정보 + 수강 코드 대화형 입력 |
| `npm run init-env` | — | `.env.local` 템플릿 생성 (키설정 내부용) |
| `npm run check-wordpress` | — | 워드프레스 연결 확인 |
| `npm run adsense:create-drafts` / `adsense:auto-drafts` | — | 승인글 임시글 생성 |
| `npm run save` | `저장` | 텍스트 산출물 커밋+push (push 전 시크릿 검사 포함) |
| `npm run update:kit` | `업데이트` | 템플릿 저장소에서 프로그램 영역만 갱신 |

위 명령은 모두 구현되어 있다. 실행이 "파일을 찾을 수 없다"로 실패하면(구버전 작업방) 대체 방법을 임의로 만들지 말고, 터미널에 `업데이트` 실행을 안내하고 그래도 안 되면 코치 문의를 안내한다. 특히 `저장`이 실패한다고 해서 Codex가 직접 `git add`/`git push`를 실행하지 않는다(push 전 시크릿 검사를 건너뛰게 되기 때문).

## 폴더 안내

- `애드센스 승인글/01_제목넣는곳`: 사이트별 제목 파일 (사이트1제목.txt … 사이트10제목.txt, 최대 10개)
- `애드센스 승인글/02_생성결과_확인용`: 생성 결과 확인
- `99_절대_건들지마세요_프로그램파일`: 자동화 프로그램 (작업 기준 폴더, 세부 규칙 AGENTS.md 포함)
- `docs/`: 수강생·코치용 참고 문서 (키 발급 가이드 등)

## 완료 보고

작업이 끝나면 아래만 짧게 말한다.

- 무엇을 몇 개 만들었는지
- 이미 쓴 제목을 건너뛰었다면 그 사실만 한 줄 (몇 개를 조회했는지 같은 과정은 말하지 않는다)
- 다음에 할 수 있는 것 한 가지
- **돈은 반드시.** 스크립트가 찍은 `💰` 줄의 숫자를 그대로 옮긴다 — 이 글(또는 이번 실행)에 쓴 토큰과 원, 충전액 대비 퍼센트, 지금까지 쓴 돈, 남은 돈. 요약하거나 빼지 않는다. 남은 돈이 안 나왔으면 "'키설정'에서 충전한 금액을 넣으면 남은 돈도 보여 드려요"를 한 줄 붙인다.

말하지 않는다 — 수강생이 몰라도 되는 것들:
- 폴더 경로(`애드센스 승인글/02_…` 같은 내부 경로). "워드프레스 임시글에서 보세요"로 충분하다.
- 날짜를 어떻게 정했는지. 사용자가 날짜를 직접 지정했을 때만 그것을 확인해 준다.
- 내부 파일명·옵션명·조회 건수 같은 과정
